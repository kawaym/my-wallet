import { saira_stencil_one } from "./fonts";

export default function Logo() {
  return (
    <p
      className={`${saira_stencil_one.className} font-normal text-2xl md:text-6xl`}
    >
      MyWallet
    </p>
  );
}
