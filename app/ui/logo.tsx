import { saira_stencil_one } from "./fonts";

export default function Logo() {
  return (
    <p
      className={`${saira_stencil_one.className} font-normal text-3xl md:text-6xl mb-3 text-white`}
    >
      MyWallet
    </p>
  );
}
